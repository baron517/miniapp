<view class="container" wx:if="{{styles===1}}">
    <view bindtap="navToPage" class="list_content column1" data-linkparam="{{item.link_param}}" data-linktype="{{item.link_type}}" wx:if="{{column===1}}" wx:for="{{lists}}">
        <image mode="widthFix" src="{{item.img}}" style="border-radius:{{radius}}%;"></image>
        <view class="list_title">{{item.title}}</view>
    </view>
    <view bindtap="navToPage" class="list_content column2" data-linkparam="{{item.link_param}}" data-linktype="{{item.link_type}}" wx:if="{{column===2}}" wx:for="{{lists}}">
        <image mode="widthFix" src="{{item.img}}" style="border-radius:{{radius}}%;"></image>
        <view class="list_title">{{item.title}}</view>
    </view>
    <view bindtap="navToPage" class="list_content column3" data-linkparam="{{item.link_param}}" data-linktype="{{item.link_type}}" wx:if="{{column===3}}" wx:for="{{lists}}">
        <image mode="widthFix" src="{{item.img}}" style="border-radius:{{radius}}%;"></image>
        <view class="list_title">{{item.title}}</view>
    </view>
    <view bindtap="navToPage" class="list_content column4" data-linkparam="{{item.link_param}}" data-linktype="{{item.link_type}}" wx:if="{{column===4}}" wx:for="{{lists}}">
        <image mode="widthFix" src="{{item.img}}" style="border-radius:{{radius}}%;"></image>
        <view class="list_title">{{item.title}}</view>
    </view>
</view>
<view class="container" wx:if="{{styles===2}}">
    <view bindtap="navToPage" class="list_contents column5" data-linkparam="{{item.link_param}}" data-linktype="{{item.link_type}}" style="border-radius:{{radius}}rpx;" wx:if="{{column===1}}" wx:for="{{lists}}">
        <image mode="widthFix" src="{{item.img}}" style="border-radius:{{radius}}rpx;"></image>
        <view class="list_titles">{{item.title}}</view>
    </view>
    <view bindtap="navToPage" class="list_contents column6" data-linkparam="{{item.link_param}}" data-linktype="{{item.link_type}}" style="border-radius:{{radius}}rpx;" wx:if="{{column===2}}" wx:for="{{lists}}">
        <image mode="widthFix" src="{{item.img}}" style="border-radius:{{radius}}rpx;"></image>
        <view class="list_titles">{{item.title}}</view>
    </view>
    <view bindtap="navToPage" class="list_contents column7" data-linkparam="{{item.link_param}}" data-linktype="{{item.link_type}}" style="border-radius:{{radius}}rpx;" wx:if="{{column===3}}" wx:for="{{lists}}">
        <image mode="widthFix" src="{{item.img}}" style="border-radius:{{radius}}rpx;"></image>
        <view class="list_titles f28">{{item.title}}</view>
    </view>
    <view bindtap="navToPage" class="list_contents column8" data-linkparam="{{item.link_param}}" data-linktype="{{item.link_type}}" style="border-radius:{{radius}}rpx;" wx:if="{{column===4}}" wx:for="{{lists}}">
        <image mode="widthFix" src="{{item.img}}" style="border-radius:{{radius}}rpx;"></image>
        <view class="list_titles f28">{{item.title}}</view>
    </view>
</view>
<view class="container" wx:if="{{styles===3}}">
    <view bindtap="navToPage" class="list_content column1" data-linkparam="{{item.link_param}}" data-linktype="{{item.link_type}}" wx:if="{{column===1}}" wx:for="{{lists}}">
        <image mode="widthFix" src="{{item.img}}" style="border-radius:{{radius}}rpx;"></image>
    </view>
    <view bindtap="navToPage" class="list_content column2" data-linkparam="{{item.link_param}}" data-linktype="{{item.link_type}}" wx:if="{{column===2}}" wx:for="{{lists}}">
        <image mode="widthFix" src="{{item.img}}" style="border-radius:{{radius}}rpx;"></image>
    </view>
    <view bindtap="navToPage" class="list_content column3" data-linkparam="{{item.link_param}}" data-linktype="{{item.link_type}}" wx:if="{{column===3}}" wx:for="{{lists}}">
        <image mode="widthFix" src="{{item.img}}" style="border-radius:{{radius}}rpx;"></image>
    </view>
    <view bindtap="navToPage" class="list_content column4" data-linkparam="{{item.link_param}}" data-linktype="{{item.link_type}}" wx:if="{{column===4}}" wx:for="{{lists}}">
        <image mode="widthFix" src="{{item.img}}" style="border-radius:{{radius}}rpx;"></image>
    </view>
</view>
